# Port to listen on (cloud providers like Render / Railway automatically set $PORT)
PORT=8000

# Comma-separated list of allowed origins for CORS
# In production, set to your Vercel frontend URL (or leave empty to allow *.vercel.app)
CORS_ORIGINS=http://localhost:5173,http://localhost:3000,https://regacore-blood-dashboard.vercel.app
